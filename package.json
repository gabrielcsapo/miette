{
  "name": "root",
  "private": true,
  "scripts": {
    "lint": "lerna run lint",
    "playground": "cd packages/miette && npm run playground",
    "prepare": "husky install && lerna bootstrap"
  },
  "devDependencies": {
    "husky": ">=6",
    "lerna": "^4.0.0"
  }
}
